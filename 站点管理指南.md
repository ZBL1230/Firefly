# ZBL 神奇宝库 · 站点管理指南

> 站点：https://zblweb.top ｜ 框架：Astro + Firefly（Fuwari 二次开发）
> 本指南面向站长本人，涵盖日常维护需要用到的所有基础知识。

---

## 1. 技术栈与目录结构

| 项 | 值 |
|---|---|
| 前端框架 | Astro |
| 主题 | Firefly（基于 Fuwari 二次开发） |
| 样式 | Tailwind CSS |
| 组件 | Svelte |
| 包管理 | pnpm |
| 内容存储 | Markdown / MDX（`src/content/`） |
| 站点配置 | TypeScript 配置文件（`src/config/`） |

```
Firefly/
├── src/
│   ├── config/          # 站点配置文件（核心，见第 2 节）
│   ├── content/         # 内容
│   │   ├── posts/       # 文章（.md/.mdx）
│   │   ├── dynamic/     # 动态（.md）
│   │   ├── projects/    # 项目（.md/.mdx）
│   │   └── spec/        # 单页内容（about.md / friends.mdx / guestbook.md）
│   ├── components/      # 可复用组件
│   ├── pages/           # 页面路由
│   ├── assets/          # 图片等静态资源（会被打包）
│   └── types/           # TypeScript 类型
├── public/              # 直接复制到根目录的静态资源（不会被 hash）
├── scripts/             # 构建脚本
├── dist/                # 构建产物（勿手改，上线时上传这个）
├── image-backup-*/      # 旧图备份
└── package.json         # 依赖与脚本
```

> **`src/assets/` 与 `public/` 的区别**：`src/assets/` 里的图会被 Astro 打包 + 加 hash（适合正文/头像）；`public/` 里的图原样复制到根目录（适合配置里用固定路径引用的图，如打赏二维码 `/assets/images/sponsor/*.png`）。

---

## 2. 核心配置文件速查表

`src/config/` 下的每个文件管一块功能，改完需 `pnpm build` 生效。

| 文件 | 管什么 | 常用字段 |
|---|---|---|
| `siteConfig.ts` | 站点基础 & 主题 | `title` 站名、`url` 域名、`themeColor.hue` 主题色相(0-360)、`keywords`、`subtitle`、`mal.username` |
| `profileConfig.ts` | 我的信息 | `name`、`avatar` 头像、`bio` 签名、`qq`、`email`、`github`、`agreement` |
| `commentConfig.ts` | 评论系统 | `type`（`"none"`/`"giscus"` 等）—— 当前为 `"none"`，评论区显示"维护中" |
| `sponsorConfig.ts` | 打赏 | `methods` 收款方式（微信/支付宝二维码）、`sponsors` 打赏者列表 |
| `friendsConfig.ts` | 友链 | `friendsConfig` 数组（见第 4 节）、`randomizeSort` 随机排序开关 |
| `galleryConfig.ts` | 相册 | `albums` 相册数组（见第 4 节） |
| `announcementConfig.ts` | 公告 | `content` 公告文案、`showOnHome` |
| `booknavConfig.ts` | 书签导航 | `books` 站点收藏数组 |
| `navBarConfig.ts` | 导航栏 | `menu`、`blockchain` 子菜单等 |
| `backgroundWallpaper.ts` | 横幅/背景 | `homeText.title/subtitle` 打字机文案、`links` 社交图标、背景图 |
| `footerConfig.ts` | 页脚 | `content` HTML 注入 |
| `sidebarConfig.ts` | 侧边栏 | 侧边栏模块开关与顺序 |
| `displaySettingsConfig.ts` | 展示设置 | 列表/网格布局、封面等 |
| `effectsConfig.ts` | 页面特效 | 樱花、水波、烟花等开关 |
| `musicConfig.ts` | 背景音乐 | 播放器开关与歌单 |
| `analyticsConfig.ts` | 统计 | 百度统计/Google 等 ID |
| `dynamicConfig.ts` | 动态页 | `apiUrl`、`memos`（对接 Memos 动态源） |
| `coverImageConfig.ts` | 封面 | 文章默认封面图 |
| `fontConfig.ts` | 字体 | 正文霞鹜文楷 + 代码 Maple Mono，详见 **第 8 节** |
| `licenseConfig.ts` / `expressiveCodeConfig.ts` / `mermaidConfig.ts` / `plantumlConfig.ts` / `pioConfig.ts` | 版权 / 代码高亮 / 图表 | 按需调整 |

> 配置文件是 TypeScript，改完**数值或字符串**即可。为安全起见，改动后跑一遍 `pnpm build` 确认没有语法错误。

---

## 3. 常用命令

```bash
# 安装依赖（首次）
pnpm install

# 本地开发预览（默认 http://localhost:4321）
pnpm dev

# 生产构建（生成 dist/，含 lqips、github-cards 等前置脚本）
pnpm build

# 本地预览构建产物（模拟线上）
pnpm preview

# 类型检查
pnpm type-check

# 代码格式化 / Lint
pnpm format
pnpm lint
```

| 命令 | 说明 |
|---|---|
| `pnpm new-dynamic` | 交互式新建一篇动态 |
| `pnpm new-post` | 交互式新建一篇文章 |
| `pnpm lqips` | 单独重新生成低质量图片占位 |
| `pnpm github-cards` | 单独重新生成 GitHub 卡片数据 |

---

## 4. 内容管理（重点）

### 4.1 文章 `src/content/posts/*.md`

```md
---
title: "文章标题"
published: 2026-09-09
draft: false            # true = 只在自己预览可见，全网不发布
comment: true           # 是否开评论
---
正文（Markdown）
```

- **发布**：把 `draft` 改为 `false`，`pnpm build` 即上线。
- **隐藏**：`draft: true`。
- 当前 `src/content/posts/` 下是模板自带的示例文章，均已标为草稿，不会发布。

### 4.2 动态 `src/content/dynamic/*.md`

```md
---
published: 2026-09-09 20:00:00
location: 北京            # 可选
pinned: false            # 可选，是否置顶
---
动态正文（支持 Markdown、图片）
```

- 动态页数据来自 `/api/dynamic.json`，构建时自动生成，无需手动改 JSON。
- 文件命名建议 `YYYY-MM-DD-HHMMSS.md`（slug 取文件名）。

### 4.3 项目 `src/content/projects/*.md`

```md
---
title: "项目名"
slug: zblai
published: 2026-09-09
order: 90                 # 数字越大越靠前
description: "一句话简介"
image: ""                 # 封面图；留空显示默认火箭图标
status: "published"       # planning=计划中 / developing=开发中 / published=已发布 / archived=已归档
tags: ["Flask", "AI"]
link:                     # 外链按钮（可选）
  - label: "访问网站"
    icon: "material-symbols:open-in-new"
    value: "https://ai.zblweb.top"
---
正文 = 项目 README，渲染在详情页底部
```

### 4.4 友链 `src/config/friendsConfig.ts`

```ts
export const friendsConfig: FriendLink[] = [
    {
        title: "站名",             // 必填
        imgurl: "",                // 头像 URL；留空显示灰色 account-circle 占位
        desc: "一句话介绍",         // 建议 ≤20 字
        siteurl: "https://xxx.com",
        tags: ["Blog"],            // 最多显示 3 个，别写空字符串
        weight: 10,                // 越大越靠前
        enabled: true,             // false = 临时隐藏（不删除）
    },
];
```

### 4.5 关于页 `src/content/spec/about.md`

- `# 关于我` 标题 + `## 🛠️ 关于本站` 说明 + 网址/开源链接。改这个文件即可。

### 4.6 相册 `src/config/galleryConfig.ts`

```ts
albums: [
    {
        id: "my-album",            // 对应 public/gallery/my-album/ 目录
        name: "相册名",
        description: "简介",
        location: "拍摄地",
        date: "2026-01-01",
        tags: ["标签"],
        password: "",              // 设定访问密码（可选）
        passwordHint: "",
    },
],
```

- **记得在 `public/gallery/<id>/` 下放入对应图片**，否则相册列表空但目录空。

---

## 5. 图片资源规范（ZBL 约定）

| 用途 | 位置 | 命名 |
|---|---|---|
| 头像 | `src/assets/images/avatar.png` | `avatar.png` |
| 桌面背景 | `src/assets/images/DesktopWallpaper/` | `zbl-scenery-lake/forest/sea/spring.webp` |
| 移动背景 | `src/assets/images/MobileWallpaper/` | `zbl-scenery-cloudsea/waterfall.webp` |
| 打赏二维码 | `public/assets/images/sponsor/` | `wechat.png` / `alipay.png` |

> 背景图命名务必用 `zbl-scenery-*`，**不要用** `d1-d6` / `m1-m6`（模板默认名，避免被覆盖）。

---

## 6. 部署上线流程

1. 本地改好内容/配置
2. `pnpm build` 确认无报错（生成 `dist/`）
3. 将 `dist/` 上传到你的托管平台（Vercel / Netlify / Cloudflare Pages / EdgeOne Pages 等），或对你的服务器指定构建
4. 平台框架预设选 `Astro`，根目录 `./`，输出目录 `dist`，构建命令 `pnpm run build`

> **构建命令务必用 `pnpm run build`**（完整版），而不是 `npx astro build`——完整版会额外跑 lqips / github-cards / subset-fonts / pagefind 等，确保图片占位、GitHub 卡片、字体子集化、站内搜索都能正常生成。

---

## 7. 常见问题排查

### 7.1 改了内容/配置但线上没生效？
大概率是 **content collection 缓存**。`node_modules/.astro/data-store.json` 会缓存文章/动态/项目数据，删了文件后直接 build 仍会用旧数据。**完整清理命令**：

```bash
rm -rf node_modules/.astro node_modules/.vite dist .astro
pnpm build
```

（`build` 是常驻缓存；`dist` 是旧产物；`.astro` 是项目级生成缓存。）

### 7.2 文章页没生成？
生产中 `draft: true` 的文章不发布。检查文章 frontmatter 的 `draft` 是否为 `false`。

### 7.3 想大改主题色？
改 `siteConfig.ts` 的 `themeColor.hue`（0-360），全站 7 个色阶自动重新派生。当前为 **168（翡翠碧）**。

### 7.4 评论怎么开？
把 `commentConfig.ts` 的 `type` 从 `"none"` 改为 `"giscus"` 并填入 repo 等配置。当前显示"评论功能正在维护中，敬请期待。"。

### 7.5 新增内容后有哪些注意事项？
- 文章/动态/项目用 content 目录里的 `.md` 文件（带 frontmatter）
- 友链、相册、打赏、导航、横幅等用 `src/config/` 里的数组
- 改完跑 `pnpm build`，上报平台前确认 `dot` 或几处关键页面正常

### 7.6 Cloudflare Pages 部署报 `wrangler name` 不合法？
错信息形如：`Expected "name" to be of type string, alphanumeric and lowercase with dashes only but got "你的名字"`
- 原因：`wrangler.jsonc` / `wrangler.toml` 里 `name` 用中文或大写字母。wrangler 强制要求 name 是 **小写字母 + 数字 + 连字符**
- 解决：把 name 改成拼音或英文（如 `zbl-magic-vault`），然后 commit + push 重新触发部署
- ⚠️ 建议同步修改两个 wrangler 文件（如果两个都存在），避免下次换 wrangler 配置文件时再报错

---

## 8. 字体方案（霞鹜文楷 + 马善政 + Maple Mono）

| 用途 | 字体 | 来源 | 配置位置 |
|---|---|---|---|
| 全站正文 / 标题 | **霞鹜文楷 LXGW WenKai**（Regular 400 + Bold 700） | 自托管分片 webfont | `fontConfig.ts` → `selfHostedFonts` |
| 主页横幅主标题 | **马善政毛笔楷书** | 本地 + 构建时子集化（约 33KB） | `fontConfig.ts` → `bannerTitleFont` |
| 代码块 / 行内代码 | **Maple Mono**（带连字） | Fontsource（Astro Font API） | `fontConfig.ts` → `fontsList` + `codeFont` |
| 横幅副标题 | Inter（拉丁）+ 文楷（中文） | Fontsource | `fontConfig.ts` → `bannerSubtitleFont` |

> Zen Maru Gothic 仍留在 `fontsList` 里备用（改 `bannerTitleFont` 即可切回），当前未被使用、不会加载。

### 8.1 为什么中文正文不走 Astro Font API？

中文字体有两个硬约束：

1. **Fontsource 上的中文包基本只有 latin 子集**（如 `@fontsource/lxgw-wenkai` 就只发布了西文字形），中文会缺字；
2. 完整中文字体动辄 5~20MB，整包加载会严重拖慢首屏。

所以正文改用**分片（unicode-range）webfont**：字体被切成 97 个小分片，浏览器只下载当前页面文字实际命中的分片（通常 10~25 个，约 200~800KB），中文全覆盖，评论/搜索等动态内容也不会缺字。

### 8.2 目录结构

```
public/assets/fonts/lxgw-wenkai/
├── lxgw-wenkai.css    194 条 @font-face（400 + 700），由官方包 CSS 生成，勿手改
├── files/*.woff2      194 个分片（每字重 97 个，带 unicode-range）
└── OFL.txt            字体授权（SIL Open Font License 1.1，可商用）
```

`src/config/fontConfig.ts` 末尾的 `selfHostedFonts` 负责把 CSS 挂进 `<head>`，并把字体家族名插进 body 字体栈（位置在 `selected` 字体之后、系统字体之前）。加载逻辑在 `src/components/features/FontSetup.astro`。

### 8.3 换字体怎么做

1. 从 [npm](https://www.npmjs.com/package/lxgw-wenkai-webfont) 下载分片包（如 `lxgw-wenkai-webfont@1.7.0`）
2. 把 `files/*.woff2` 拷到 `public/assets/fonts/<新目录>/files/`
3. 把包里的 `<weight>.css` 拼成一个 CSS，`url('./files/xx.woff2')` 改成 `url('/assets/fonts/<新目录>/files/xx.woff2')`
4. 改 `fontConfig.ts` 的 `selfHostedFonts`：`family` = CSS 里的 `font-family`，`cssHref` = CSS 路径

### 8.4 横幅标题字体（马善政）怎么改文案

横幅文案在 `src/config/backgroundWallpaper.ts` 的 `common.homeText.title` / `subtitle`。

马善政完整中文字集约 2.6MB，而横幅只有固定几十个字，所以做了**两级子集化**：

1. 先由 `scripts/subset-banner-font.ts` 把源字体裁到「横幅文案 + 标点 + ASCII」，约 **33KB**，输出到 `public/assets/fonts/MaShanZheng-Banner.woff2`
2. 构建时 `scripts/subset-fonts.ts` 再在它基础上子集化一次（体积只会更小）

**改了横幅文案要重跑**：

```bash
npx tsx scripts/subset-banner-font.ts
```

忘了跑也不会出方框 —— 新增的字符会退回霞鹜文楷显示，只是字形不统一。

> 为什么不直接用内置子集化？内置的是按「全站字符」切，实测要 **197KB**；而横幅只用一个页面、几十个字，33KB 足够。

### 8.5 注意事项

- ⚠️ **升级 Firefly 主题后**，`fontConfig.ts` 与 `FontSetup.astro`（`selfHostedFonts` 相关部分）可能被覆盖，需要重新加回
- 首屏 194 个分片文件只下载命中的那几个，**不要**全部 preload
- 想省流量可只保留 Regular，删掉 Bold 分片（标题会变成浏览器合成的伪粗体，观感略差）
- 字体文件已提交进仓库（文楷约 9MB + 横幅子集 33KB），换字体时记得一并替换
- 马善政的字面声明为 `weight: "700"`，因为 `.banner-title` 是 `font-weight: bold`；改成 400 会让浏览器叠一层合成粗体，毛笔笔画会糊

---

## 9. 可继续 DIY 的方向（可选）

- 横幅打字机文案：`backgroundWallpaper.ts` 的 `homeText.subtitle[]`
- 主题色：`themeColor.hue`
- 礼物/彩蛋：`effectsConfig.ts`
- 站内搜索：依赖 pagefind，`pnpm build` 自动生成

---

*本文档随站点维护持续更新，建议每次大改动后同步修订。*
